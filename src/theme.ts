import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: { primary: { main: '#219653' } },
  typography: { fontFamily: 'Montserrat' },
});

export default theme;
